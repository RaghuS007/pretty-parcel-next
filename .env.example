# --- Database (Neon Postgres) ---------------------------------
# Leave unset for local dev: the app runs on an in-memory store.
# DATABASE_URL=postgresql://user:pass@ep-xxx.ap-southeast-1.aws.neon.tech/tpp?sslmode=require

# --- OTP / SMS (MSG91) ----------------------------------------
# Leave unset for local dev: OTP is fixed to 123456 and logged to console.
# MSG91_AUTH_KEY=
# MSG91_TEMPLATE_ID=
# MSG91_SENDER_ID=TPPNMS

# --- Sessions --------------------------------------------------
AUTH_SECRET=change-me-in-production
